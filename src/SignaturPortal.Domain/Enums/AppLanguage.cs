namespace SignaturPortal.Domain.Enums;

/// <summary>
/// Language identifiers matching legacy GenericObjects.Language values.
/// Note: value 2 is intentionally skipped (legacy gap).
/// </summary>
public enum AppLanguage
{
    Default = 0,
    EN = 1,
    DK = 3,
    DE = 4,
    ES = 5
}
